apiVersion: v1
kind: Service
metadata:
  name: chat-server
spec:
  type: NodePort
  selector:
    app: chat-server
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
      nodePort: 30051
    - name: metrics
      port: 9090
      targetPort: 9090
