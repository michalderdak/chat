apiVersion: v1
kind: Service
metadata:
  name: chat-gateway
spec:
  type: NodePort
  selector:
    app: chat-gateway
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
