apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-server
spec:
  template:
    spec:
      containers:
        - name: chat-server
          env:
            - name: AUTH_ENABLED
              value: "false"
